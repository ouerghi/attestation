{% extends 'base.html.twig' %}

{% block body %}
    <h1>Typeattestation</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ typeAttestation.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ typeAttestation.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ typeAttestation.description }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ typeAttestation.code }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('typeattestation_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('typeattestation_edit', { 'id': typeAttestation.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
